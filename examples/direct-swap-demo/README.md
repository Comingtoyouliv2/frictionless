# Direct swap demo

This example will run two mock providers. Each provider owns a separate local credit store and communicates through signed direct-swap messages.

The first scenario is a QR or link-based one-to-one exchange:

~~~
Provider A credit 10 <-> Provider B credit 5
~~~

No Frictionless-hosted credit database is required.
